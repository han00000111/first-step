# 本地开发默认使用 Prisma + SQLite
DATABASE_URL="file:./dev.db"

# Vercel 公网演示的最小可行方案：
# 1. 在 Vercel 项目环境变量中把 DATABASE_URL 改成 file:/tmp/first-step-demo.db
# 2. 运行时会把仓库里的 prisma/demo.db 快照复制到 /tmp，供当前实例读写
# 3. 不要在 Vercel 上使用 file:./dev.db
#
# DATABASE_URL="file:/tmp/first-step-demo.db"
